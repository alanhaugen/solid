#include "ndkinput.h"

ndkinput::ndkinput() {
	/*Start  = Button(VK_ESCAPE);
	Select = Button(VK_RETURN);
	Up     = Button(VK_UP);
	Down   = Button(VK_DOWN);
	Left   = Button(VK_LEFT);
	Right  = Button(VK_RIGHT);
	Fire   = Button(VK_SPACE);
	Jump   = Button(VK_CONTROL);
	Alt1   = Button(VK_SHIFT);
	Alt2   = Button(VK_MENU);*/

	// Add the Windows specific buttons to Input list
	//Input.push_back(Select);
}